*NOTE: I am using Debian 12 when making this reference* 

-Installation:
You want to install Vim GUI version eg. Vim GTK then check if it is installed properly
>which vim

-Create vim config file (if not already created)
cd $HOME
ls -a (check if already created)
touch .vimrc (if not already created)





